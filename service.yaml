kind: Service
apiVersion: v1
metadata:
  name: react-service
  namespace: react-app
spec:
  type: NodePort
  selector:
    app: react-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30008
